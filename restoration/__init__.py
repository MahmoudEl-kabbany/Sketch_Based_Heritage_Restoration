"""Heritage Sketch Restoration Engine.

Simple API:
    from restoration.pipeline import restore, restore_batch

    result = restore("path/to/damaged_sketch.png")
    results = restore_batch(["img1.png", "img2.png"])
"""
